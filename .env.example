# 本番ビルド専用（Vercel の Environment Variables に同じ名前で設定する）
# Git に master_data.csv を入れないとき、ビルド直前に1回取得して public/data/ に保存する
# 例: S3 署名付きURL、社内の一時配布URL など。ローカルでは未設定で既存の public/data を使用可
# MASTER_CSV_BUNDLE_URL=
